/** @type {import('./$types').LayoutLoad} */
export async function load() {
   return {
      pageTitles: {
         home: "Pacific Northwest Thought Experiment",
         blog: "Blog",
         about: "About",
         contact: "Contact"
      }
   };
}
